{% comment %} Example adding layout for a new module to existing Flowbite Library (#5 or #6). For libraries with a paid and free version, always add to the paid version's folder, in this case #6, then if the layout is free, reference in both layouts JSON files.  {% endcomment %}

{%- raw -%}
<section>
  <div>
    {%- include 'modules/siteglide_system/get/get_items', item_layout: 'item' -%}
  </div>
</section>
{%- endraw -%}